🎮 AI游戏买手 · 产品设计文档 v1.0
1. 产品定位
一句话描述
一个“懂情绪、有品味、会主动推荐”的AI游戏买手，帮助玩家在信息过载的时代快速找到真正想玩的游戏。
核心特色
•	多智能体讨论机制：三个不同视角的Agent（硬核、艺术、省钱）先“内部讨论”，再由主持人汇总成一段有温度的朋友式推荐。
•	情绪/场景匹配：不止基于类型，更基于用户当前的心情和场景。
•	跨平台数据：整合Steam、Epic、PSN、Switch等平台信息，包括价格、折扣、评分。
目标用户
•	普通玩家：面对海量游戏不知道玩什么
•	硬核玩家：想发掘冷门佳作
•	纠结型玩家：希望有人帮做决策
________________________________________
2. 用户交互流程
用户通过网页聊天界面与买手互动，典型对话流程如下：
1.	用户提问
o	例如：“推荐几款像《星露谷物语》的游戏”
o	或：“今天心情不好，想找个解压的”
2.	系统触发多Agent并行思考
o	后台同时唤醒三个Agent：硬核顾问、艺术顾问、省钱顾问
o	每个Agent根据用户意图和自己的专业领域，从游戏库中检索并生成一段推荐理由
3.	主持人Agent汇总
o	收集三个Agent的输出，整合成一段连贯、自然的回复
o	回复包含：各Agent的观点摘要 + 最终综合建议 + 相关游戏链接/按钮
4.	用户反馈
o	用户可以选择“喜欢”或“再推荐”，或继续追问
________________________________________
3. 技术架构设计
采用轻量级微服务架构，一人公司可快速搭建。
text
复制
下载
[前端] 网页聊天界面 (HTML/CSS/JS) 
        ↓ (HTTP)
[后端] Flask/FastAPI 主应用
        ├── 意图识别模块 (调用LLM，如GPT-4o-mini)
        ├── 任务分发器
        ├── Agent 1: 硬核顾问 (调用LLM + 游戏库API)
        ├── Agent 2: 艺术顾问 (调用LLM + 游戏库API)
        ├── Agent 3: 省钱顾问 (调用LLM + 游戏库API + 价格API)
        └── 主持人Agent (调用LLM，汇总生成回复)
[数据层] 游戏库 (初期可用RAWG/IGDB API + 本地缓存)
[外部API] 
    - 大模型API (OpenAI/DeepSeek/Claude等)
    - 游戏数据API (RAWG, IGDB)
    - 价格API (Steam官方API, 或第三方如IsThereAnyDeal)
3.1 核心组件说明
组件	职责	技术实现提示
前端	提供聊天界面，发送用户消息，接收并展示回复	可用Streamlit快速搭建，或纯HTML+JS
意图识别	分析用户输入，提取游戏名、情绪、预算等关键信息	调用LLM，设计Prompt
任务分发器	并行调用三个Agent，并等待结果	用Python的asyncio或线程池
硬核顾问	从游戏机制、难度、系统深度角度推荐	调用LLM，配合游戏库检索
艺术顾问	从美术、音乐、叙事、氛围角度推荐	同上
省钱顾问	考虑价格、折扣、性价比，提醒省钱策略	需接入价格API
主持人	汇总三个Agent的推荐，生成连贯回复	调用LLM，设计Prompt
游戏库	存储游戏元数据（名称、类型、标签、价格、评分等）	初期用RAWG API，后续可本地缓存
________________________________________
4. Agent提示词设计
以下是各Agent的Prompt模板，可根据实际模型微调。
4.1 意图识别模块
输入：用户消息
输出：结构化意图（JSON）
text
复制
下载
你是一个游戏推荐助手的意图识别专家。请从用户消息中提取以下信息，并以JSON格式返回：
- game_name: 提到的游戏名（如果有）
- emotion: 情绪关键词（如“累”、“开心”、“无聊”等）
- scenario: 场景（如“一个人玩”、“情侣”、“短时间”等）
- preferences: 其他偏好（如“像素风”、“策略”、“剧情”等）
- price_range: 价格范围（如有）
- platform: 平台偏好（如有）

用户消息：{用户输入}
4.2 硬核顾问 Agent
角色设定：资深硬核玩家，关注游戏机制、操作深度、系统复杂度、耐玩性。
text
复制
下载
你是一个硬核游戏顾问，只从游戏机制、难度、系统深度、可重玩性角度推荐游戏。  
用户意图：{意图JSON}  
游戏库数据：{相关游戏列表}  

请根据用户意图，从硬核角度推荐3款游戏。对每款游戏，说明为什么它的机制/深度适合用户，并给出你的专业见解。  
输出格式：一段自然语言，不要太长，但要体现硬核感。
4.3 艺术顾问 Agent
角色设定：游戏艺术爱好者，关注美术风格、音乐、叙事、氛围营造。
text
复制
下载
你是一个艺术型游戏顾问，只从美术风格、音乐、叙事、氛围角度推荐游戏。  
用户意图：{意图JSON}  
游戏库数据：{相关游戏列表}  

请根据用户意图，从艺术角度推荐3款游戏。描述它们的美术特点、音乐如何烘托情绪、故事是否动人。  
输出格式：一段自然语言，有文艺气息，但不要过于抽象。
4.4 省钱顾问 Agent
角色设定：精明玩家，关注价格、折扣、性价比，提醒避坑。
text
复制
下载
你是一个省钱型游戏顾问，只从价格、折扣、性价比、是否值得现在买的角度推荐游戏。  
用户意图：{意图JSON}  
游戏库数据：{相关游戏列表}  
价格信息：{价格API返回}  

请推荐1-3款游戏，强调它们的当前价格、史低情况、是否值得入手，以及如果现在不买建议何时买。  
输出格式：一段自然语言，带点“精明”的语气。
4.5 主持人 Agent
角色设定：总结者，将三个顾问的观点融合成一段连贯、友好的回复。
text
复制
下载
你是一个游戏买手主持人，需要将三位顾问（硬核、艺术、省钱）的观点整合成一段自然流畅的回复，以朋友的口吻推荐给用户。  
用户原话：{用户输入}  
硬核顾问观点：{硬核输出}  
艺术顾问观点：{艺术输出}  
省钱顾问观点：{省钱输出}  

请按以下结构组织：
1. 先简短回应一下用户的情绪或需求。
2. 分别引用三位顾问的精华，可以说“硬核顾问觉得...”、“艺术顾问更看重...”、“省钱顾问提醒你...”。
3. 最后给出你的综合建议，并附上一个小结（比如“综合来看，今天最适合你的是...”）。
4. 回复要亲切、像朋友聊天，但不要啰嗦。
________________________________________
5. 游戏库设计（MVP版）
初期为了快速验证，不必自建完整数据库。采用 RAWG API + 本地缓存 的方式。
5.1 数据获取流程
•	用户提问后，根据意图中的游戏名或标签，调用 RAWG 的 /games 接口搜索相关游戏。
•	对返回的每款游戏，提取：名称、封面图、评分、类型、标签、平台、简介。
•	将结果存入本地缓存（如SQLite或Redis），避免重复请求。
5.2 数据增强
•	手动为少量热门游戏添加“情绪标签”（如“治愈”、“解压”、“烧脑”等），用于展示特色。
•	后期可考虑引入SteamSpy或IGDB的API补充数据。
________________________________________
6. MVP实现步骤（分阶段）
第1周：单人Agent原型
•	搭建简单前端聊天框（Streamlit或HTML）
•	实现意图识别（调用GPT）
•	只调用一个硬核顾问，从RAWG获取同类游戏
•	直接返回推荐列表（无主持人）
•	验证用户是否愿意用
第2周：双Agent（硬核+省钱）
•	增加省钱顾问，集成Steam价格API（或使用IsThereAnyDeal）
•	回复中加入价格信息
•	观察用户对价格的反馈
第3周：三Agent + 主持人
•	加入艺术顾问
•	实现主持人Agent整合输出
•	设计“三段式”回复风格
第4周：优化与测试
•	收集用户对话日志，优化Prompt
•	增加“再推荐”按钮
•	考虑加入用户记忆（简单用Session存历史）
________________________________________
7. 如何体现“买手”特色
•	主动推送（后期）：基于用户历史，每周推送一条精选，如“你喜欢的《Hades》开发商新作…”。
•	记忆能力（后期）：记住用户玩过的游戏、喜欢的方向，在对话中体现。
•	人格化名字和头像：让用户感觉在和一个真实的朋友对话。
•	透明讨论：展示三个顾问的“观点交锋”，增加趣味性和信任感。
________________________________________
8. 扩展方向
•	加入用户登录：保存偏好，跨会话记忆。
•	接入更多平台数据：Epic、GOG、Switch等。
•	社区功能：用户可分享推荐结果，形成UGC。
•	移动App：方便随时问。

